# DriveTree

DriveTree는 브라우저에서 폴더를 선택하면 파일 크기를 집계해서 WizTree 스타일의 트리맵으로 보여주는 가벼운 디스크 분석기입니다.

## 실행

```bash
npm start
```

브라우저에서 `http://localhost:3000` 을 열고 폴더를 선택하면 됩니다.

## 테스트

```bash
npm test
```
